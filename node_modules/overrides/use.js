"use strict";

var React = require("react");

var _require = require("./core"),
    initializeOverrideWrappers = _require.initializeOverrideWrappers;

function useOverrides(defaultComponents, overrides) {
  var overridesRef = React.useRef(overrides);
  overridesRef.current = overrides;
  return React.useMemo(function () {
    return initializeOverrideWrappers(defaultComponents, function () {
      return overridesRef.current;
    });
  }, [defaultComponents]);
}

module.exports = useOverrides;